@model PetaPoco.Page<SmartShopConnection.Product>
<div class="maincontent-area">
    <div class="zigzag-bottom"></div>
    <div class="container">
        <div class="row">
            <div class="mens-toolbar">

                <div class="pager">

                    <ul class="dc_pagination dc_paginationA dc_paginationA06">
                        @if (Model.CurrentPage != 1)
                        {
                            <li class="pre"><a href="@Url.Action("Index", "Home", new { page = Model.CurrentPage - 1})" class="previous">Lùi</a></li>
                        }
                        @if (Model.CurrentPage >= 4)
                        {
                            <li><a href="@Url.Action("Index", "Home", new { page=1})">1</a></li>
                        }
                        @if (Model.CurrentPage >= 5)
                        {
                            <li><a>...</a></li>
                        }

                        @for (int i = 1; i <= Model.TotalPages; i++)
                        {

                            if (i + 2 == Model.CurrentPage)
                            {
                                <li><a href="@Url.Action("Index", "Home", new { page = Model.CurrentPage - 2 })">@i</a></li>
                            }
                            if (i + 1 == Model.CurrentPage)
                            {
                                <li><a href="@Url.Action("Index", "Home", new { page = Model.CurrentPage - 1 })">@i</a></li>
                            }

                            if (i == Model.CurrentPage)
                            {
                                <li><a class="current" href="@Url.Action("Index", "Home", new { page=@i})">@i</a></li>
                            }
                            if (i - 1 == Model.CurrentPage)
                            {
                                <li><a href="@Url.Action("Index", "Home", new { page = Model.CurrentPage + 1 })">@i</a></li>
                            }
                            if (i - 2 == Model.CurrentPage)
                            {
                                <li><a href="@Url.Action("Index", "Home", new { page = Model.CurrentPage + 2 })">@i</a></li>
                            }
                        }
                        @if (Model.CurrentPage <= Model.TotalPages - 4)
                        {
                            <li><a>...</a></li>
                        }
                        @if (Model.CurrentPage <= Model.TotalPages - 3)
                        {
                            <li><a href="@Url.Action("Index", "Home", new { page = Model.TotalPages})">@Model.TotalPages</a></li>
                        }
                        @if (Model.CurrentPage != Model.TotalPages)
                        {
                            <li class="next"><a href="@Url.Action("Index", "Home", new { page = Model.CurrentPage + 1 })" class="previous">Tới</a></li>
                        }
                    </ul>
                    <div class="clear"></div>
                </div>
                <div class="clear"></div>
            </div>

            <div class="col-md-12">
                <div class="latest-product">
                    <h2 class="section-title">Latest Products</h2>
                    <div class="row">
                        @foreach (var item in Model.Items)
                        {
                            <div class="col-md-4 single-product @Html.DisplayFor(modelItem => item.MaSP)">
                                <div class="product-f-image">
                                    <img src="~/Image/product/@Html.DisplayFor(modelItem => item.Hinh)" alt="">
                                    <div class="product-hover">
                                        <a class="add-to-cart-link" onclick="AddToCart('@Html.DisplayFor(modelItem => item.MaSP)')">
                                            <i class="fa fa-shopping-cart"></i>
                                            Add to cart
                                        </a>
                                        <a href="/Product/Details/@Html.DisplayFor(modelItem => item.MaSP)" class="view-details-link">
                                            <i class="fa fa-link"></i>
                                            See details
                                        </a>
                                    </div>
                                </div>

                                <h2><a href="/Product/Details/@Html.DisplayFor(modelItem => item.MaSP)">@Html.DisplayFor(modelItem => item.TenSP)</a></h2>

                                <div class="product-carousel-price">
                                    <ins>@Html.DisplayFor(modelItem => item.Gia)</ins>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
        <div class="clear"></div>
    </div>
</div>
